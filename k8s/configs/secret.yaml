# Note: In production, use Kubernetes Secrets or external secret manager
# This is an example - use `kubectl create secret` for real values
apiVersion: v1
kind: Secret
metadata:
  name: hospital-secrets
  namespace: hospital-dev
type: Opaque
stringData:
  # These values should come from AWS Secrets Manager or secure storage
  DB_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  DB_USER: "postgres"
  REDIS_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  SECRET_KEY: "CHANGE_ME_IN_PRODUCTION"