apiVersion: v1
kind: ConfigMap
metadata:
  name: hospital-config
  namespace: hospital-dev
data:
  APP_HOST: "0.0.0.0"
  APP_PORT: "5000"
  FLASK_ENV: "production"
  DB_NAME: "hospital_db"
  DB_PORT: "5432"
  REDIS_PORT: "6379"
  LOG_LEVEL: "INFO"
  # RDS endpoint will be injected via environment variable
  # REDIS endpoint will be injected via environment variable